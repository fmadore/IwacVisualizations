<?php
/**
 * Visualizations: per-issue partial for Islamic periodical issues.
 *
 * Dispatched by ``Visualizations::TEMPLATE_PARTIALS`` for resource
 * template 21 (bibo:Issue — the ``publications`` HF subset, 1,501
 * issues). Renders only the asynchronous container;
 * ``asset/js/charts/publication-dashboard.js`` fetches
 * ``asset/data/publication-dashboards/{o_id}.json`` (generated by
 * ``scripts/generate_publication_dashboards.py``) and renders a
 * stat-card row, the issue's periodical-run sparkline, the
 * chronologically nearest issues of the same periodical, and — where
 * the upstream table-of-contents pipeline has coverage — semantic
 * neighbours over ``embedding_tableOfContents``.
 *
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\ItemRepresentation $resource
 */

echo $this->partial('common/iwac-block-shell', [
    'assets' => [
        'needs' => [
            'chartOptions' => true,
            'wordcloud'    => true,
            'layout'       => true,
            'renderers'    => [
                'word-cloud',
                'sibling-sparkline',
                'similar-items',
            ],
        ],
        'orchestrator' => 'publication-dashboard',
    ],
    'blockClass'   => 'iwac-vis-publication',
    'heading'      => $this->translate('Visualizations'),
    'loading'      => $this->translate('Loading visualizations'),
    'loadingClass' => 'iwac-vis-publication__loading',
    'data'         => [
        'item-id' => $resource->id(),
    ],
]);
