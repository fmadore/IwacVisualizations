<?php
/**
 * Scary Terms page block.
 *
 * Renders an interactive chart of "scary"/radical terms (terrorisme,
 * djihadisme, extrémisme, …) across the IWAC collection, with seven view
 * modes: bar chart race by year, time-series trends with historical-event
 * annotations, by country, global, co-occurrence matrix, word cloud, and
 * a MapLibre map of tagged places.
 *
 * Data is fetched from precomputed JSON files in `files/iwac-visualizations/`,
 * generated by `scripts/generate_scary_terms.py` from the Hugging Face
 * dataset `fmadore/islam-west-africa-collection` — plus the hand-curated
 * `scary-terms-events.json` annotation sidecar (committed, issue #2).
 *
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\SitePageBlockRepresentation $block
 */

echo $this->partial('common/iwac-block-shell', [
    'assets' => [
        'blockCss' => 'scary-terms',
        'needs' => [
            'chartOptions' => true,
            // Word cloud view (issue #4) — echarts-wordcloud + the shared
            // facet bar; Map view (issue #3) — the MapLibre stack.
            'wordcloud'    => true,
            'facetButtons' => true,
            'maplibre'     => true,
        ],
        // Companion modules: i18n strings + stateless builders + the three
        // view modules. All must load before the orchestrator (which
        // aliases the builders and reads the registered translations).
        'panels' => [
            'scary-terms/i18n',
            'scary-terms/helpers',
            'scary-terms/trends',
            'scary-terms/wordcloud',
            'scary-terms/map',
        ],
        'orchestrator' => 'scary-terms',
    ],
    'blockClass' => 'iwac-vis-scary',
    'embedSlug'  => 'scary-terms',
    'loading'    => $this->translate('Loading scary terms'),
    'siteBase'   => true,
]);
