<?php
/**
 * Scary Terms page block.
 *
 * Renders an interactive horizontal bar chart of "scary"/radical terms
 * (terrorisme, djihadisme, extrémisme, …) across the IWAC collection, with
 * three view modes: bar chart race by year, by country, and global.
 *
 * Data is fetched from four precomputed JSON files in `files/iwac-visualizations/`, all
 * generated by `scripts/generate_scary_terms.py` from the Hugging Face
 * dataset `fmadore/islam-west-africa-collection`.
 *
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\SitePageBlockRepresentation $block
 */

echo $this->partial('common/iwac-block-shell', [
    'assets' => [
        'blockCss' => 'scary-terms',
        'needs' => [
            'chartOptions' => true,
        ],
        // Companion modules: i18n strings + stateless builders. Both must
        // load before the orchestrator (which aliases the builders and
        // reads the registered translations).
        'panels' => [
            'scary-terms/i18n',
            'scary-terms/helpers',
        ],
        'orchestrator' => 'scary-terms',
    ],
    'blockClass' => 'iwac-vis-scary',
    'loading'    => $this->translate('Loading scary terms'),
    'siteBase'   => false,
]);
