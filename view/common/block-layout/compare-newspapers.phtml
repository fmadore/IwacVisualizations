<?php
/**
 * Compare Newspapers page block.
 *
 * Side-by-side comparison of two corpora. Each corpus is a (type, scope)
 * pair where ``type`` is articles | publications and ``scope`` is a
 * whole country or a single newspaper. The data bundles live under
 * ``asset/data/compare-newspapers/`` and are generated by
 * ``scripts/generate_compare_newspapers.py``.
 *
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\SitePageBlockRepresentation $block
 */

echo $this->partial('common/iwac-block-shell', [
    'assets' => [
        'blockCss' => 'compare-newspapers',
        'needs' => [
            'wordcloud'    => true,
            'chartOptions' => true,
            'maplibre'     => true,
            'pagination'   => true,
        ],
        // Companion modules: shared helpers (side colors + uid counter)
        // first, then one module per panel. All must load before the
        // orchestrator, which dispatches into them via
        // IWACVis.compareNewspapers.
        'panels' => [
            'compare-newspapers/helpers',
            'compare-newspapers/picker',
            'compare-newspapers/metrics',
            'compare-newspapers/overlap',
            'compare-newspapers/timeline',
            'compare-newspapers/subjects',
            'compare-newspapers/wordclouds',
            'compare-newspapers/map',
            'compare-newspapers/sentiment',
            'compare-newspapers/newspapers',
        ],
        'orchestrator' => 'compare-newspapers',
    ],
    'blockClass' => 'iwac-vis-compare-newspapers',
    'loading'    => $this->translate('Loading newspaper comparison'),
]);
