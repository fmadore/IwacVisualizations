<?php
/**
 * Compare Newspapers page block.
 *
 * Side-by-side comparison of two corpora. Each corpus is a (type, scope)
 * pair where ``type`` is articles | publications and ``scope`` is a
 * whole country or a single newspaper. The data bundles live under
 * ``asset/data/compare-newspapers/`` and are generated by
 * ``scripts/generate_compare_newspapers.py``.
 *
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\SitePageBlockRepresentation $block
 */

echo $this->partial('common/iwac-assets', [
    'blockCss' => 'compare-newspapers',
    'needs' => [
        'wordcloud'    => true,
        'chartOptions' => true,
        'maplibre'     => true,
        'pagination'   => true,
    ],
    'orchestrator' => 'compare-newspapers',
]);

$siteSlug = $this->currentSite()->slug();
?>

<div class="iwac-vis-block iwac-vis-compare-newspapers"
     data-base-path="<?= $this->escapeHtmlAttr($this->basePath()) ?>"
     data-site-base="<?= $this->escapeHtmlAttr($this->basePath() . '/s/' . $siteSlug) ?>">
    <div class="iwac-vis-loading">
        <div class="iwac-vis-spinner"></div>
        <span><?= $this->translate('Loading newspaper comparison') ?>&hellip;</span>
    </div>
</div>
