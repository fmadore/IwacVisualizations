<?php
/**
 * Index Overview page block.
 *
 * Section A — Entity Index Explorer: summary cards, type distribution,
 * top entities per type, lifespan × frequency scatter, persons gender
 * breakdown, places map (authority pins + dct:spatial mention bubbles),
 * activity gantt per type, recently added authority records, and a
 * full-text searchable/paginated index table with type facet.
 *
 * Section B — Keyword Explorer: Dublin Core Subject / Spatial Coverage
 * prevalence over time, with country + newspaper facets, top-N and
 * compare view modes, and a full all-keywords table.
 *
 * Both sections are backed by precomputed JSON:
 *     asset/data/index-overview.json               (Section A)
 *     asset/data/keyword-explorer-subjects.json    (Section B — subjects)
 *     asset/data/keyword-explorer-spatial.json     (Section B — spatial)
 *     asset/data/keyword-explorer-metadata.json    (Section B — filters)
 *
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\SitePageBlockRepresentation $block
 */

echo $this->partial('common/iwac-block-shell', [
    'assets' => [
        'blockCss' => 'index-overview',
        'needs' => [
            'maplibre'     => true,
            'chartOptions' => true,
            'facetButtons' => true,
            'table'        => true,
            'pagination'   => true,
        ],
        'panels' => [
            // Section A — Entity Index Explorer
            'index-overview/stats',
            'index-overview/type-distribution',
            'index-overview/top-entities',
            'index-overview/lifespan',
            'index-overview/places-map',
            'index-overview/activity-gantt',
            'index-overview/index-table',
            // Section B — Keyword Explorer
            'index-overview/keywords-state',
            'index-overview/keywords-filters',
            'index-overview/keywords-chart',
            'index-overview/keywords-table',
        ],
        'orchestrator' => 'index-overview',
    ],
    'blockClass' => 'iwac-vis-index-overview',
    'loading'    => $this->translate('Loading index overview'),
]);
