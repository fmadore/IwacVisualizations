<?php
/**
 * Compare Projects page block.
 *
 * Async container for side-by-side project comparison. Placeholder —
 * the orchestrator and panels are TBD. Assets are enqueued via the
 * shared `common/iwac-assets` partial; maplibre + wordcloud are
 * pre-loaded so the future orchestrator can hit the ground running.
 *
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\SitePageBlockRepresentation $block
 */

echo $this->partial('common/iwac-assets', [
    'needs' => [
        'maplibre'  => true,
        'wordcloud' => true,
    ],
]);

$siteSlug = $this->currentSite()->slug();
?>

<div class="iwac-vis-block compare-block">
    <div class="compare-container"
         data-base-path="<?= $this->escapeHtmlAttr($this->basePath()) ?>"
         data-site-base="<?= $this->escapeHtmlAttr($this->basePath() . '/s/' . $siteSlug) ?>">
        <div class="iwac-vis-loading">
            <div class="iwac-vis-spinner"></div>
            <span><?= $this->translate('Loading project comparison') ?>&hellip;</span>
        </div>
    </div>
</div>
